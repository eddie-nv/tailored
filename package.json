{
  "name": "tailored",
  "private": true,
  "scripts": {
    "dev": "pnpm -F web dev",
    "build": "pnpm -F db build && pnpm -F agents build && pnpm -F web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "db:migrate": "pnpm -F db exec prisma migrate dev",
    "db:seed": "pnpm -F db exec prisma db seed",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
